# Python environment (regenerate with: python3 -m venv .venv && .venv/bin/pip install -r requirements.txt)
.venv/
__pycache__/
*.pyc

# Swift build output (regenerate with: ./build.sh)
build/

# Generated audio
exports/
*.wav
*.mp3

# macOS
.DS_Store

# Podcast feed secrets (R2 credentials + feed token)
podcast_config.json

# podcast auto-sync watcher runtime
podcast-sync-watch.log
podcast-sync-watch.launchd.log
.podcast-sync-watch.lock/
